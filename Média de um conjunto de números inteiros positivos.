# Escreva um programa que leia uma quantidade indefinida de números inteiros positivos terminada pelo número 0 (zero). Ao final, 
# o programa deve mostrar a média aritmética de todos os números lidos (excluindo o zero).

def calculando():
    soma = 0
    numeros = 0

    while True:
        x = int(input())
        if x != 0:
            soma += x
            numeros += 1
        if x == 0: break

    if numeros > 0:
        media = soma / numeros
        print (f'{media:.2f}')

calculando()


